[package]
name = "why-cli"
version = "0.1.0"
edition = "2024"
authors = ["akira ueno"]
description = "Tells you why a command is installed on your system."
license = "MIT"

[[bin]]
name = "why"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.51", features = ["derive"] }
